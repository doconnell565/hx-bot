# Discord bot token (required)
DISCORD_TOKEN=

# Guild ID for slash command registration (optional, empty = global)
# Guild-scoped commands update instantly; global commands can take up to an hour.
DISCORD_GUILD_ID=
